import { defineMessages } from 'react-intl';

export const messages = defineMessages({
  noLiveError: {
    id: 'components.StreamSection.noLiveError',
    defaultMessage:
      'Este webinar aún no ha iniciado. Vuelve a intentarlo luego.'
  }
});
