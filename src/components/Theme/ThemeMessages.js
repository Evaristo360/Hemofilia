import { defineMessages } from 'react-intl';

export const messages = defineMessages({
  tooltip: {
    id: `components.Theme.SelectorTheme.tooltip`,
    defaultMessage: 'Cambiar a modo {theme,select,light{Obscuro}dark{Claro}}'
  }
});
